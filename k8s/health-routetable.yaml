apiVersion: gateway.solo.io/v1
kind: RouteTable
metadata:
  name: application
  namespace: gloo-system
spec:
  routes:
    - matchers:
        - prefix: /application
      routeAction:
        single:
          upstream:
            name: default-task-8080
            namespace: gloo-system

                